<!DOCTYPE html>
<html class="h-full">
  <%= render partial: "shared/head" %>
  <body class="h-full">
    <div data-controller="mobile-menu">
      <%= render partial: "shared/mobile_menu" %>
      <%= render partial: "shared/desktop_menu" %>
      <%= render partial: "shared/flash" %>
      <div class="flex flex-1 flex-col md:pl-64">
        <div class="sticky top-0 z-10 pl-1 pt-1 sm:pl-3 sm:pt-3 md:hidden bg-pink-50 border-b border-pink-200">
          <button data-action="mobile-menu#openMenu" type="button" class="-ml-0.5 -mt-0.5 inline-flex h-12 w-12 items-center justify-center rounded-md text-gray-500 hover:text-gray-900 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-pink-500">
            <span class="sr-only">Open sidebar</span>
            <!-- Heroicon name: outline/bars-3 -->
            <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
            </svg>
          </button>
        </div>
        <main class="flex-1 relative">
          <div class="py-6">
            <div class="mx-auto max-w-7xl px-4 sm:px-6 md:px-8 mt-28 sm:mt-16">
              <%= yield %>
            </div>
          </div>
        </main>
      </div>
    </div>
  </body>
</html>
